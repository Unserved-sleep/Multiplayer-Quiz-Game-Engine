# Multiplayer-Quiz-Game-Engine
# Review 1: Multiplayer Quiz Game Engine @BirdgeLabz

To-do: (Member4)
    1. Create Class Question with check(answer),id, text, category, base_points
    2. Create Class ExactQuestion - insensitive exact match
    3. Create NumericalQuestion with +-0.01 tolerance
    4. Create QuestionBank with addQuestion(), get(id), get_by_category(cat), random_quiz(n)
    5. Add Question 